{
  "apiVersion": "apps/v1",
  "kind": "Deployment",
  "metadata": {
    "name": "dns",
    "labels": {
      "app": "dns"
    }
  },
  "spec": {
    "replicas": 1,
    "selector": {
      "matchLabels": {
        "app": "dns"
      }
    },
    "template": {
      "metadata": {
        "labels": {
          "app": "dns"
        }
      },
      "spec": {
        "containers": [
          {
            "name": "unbound",
            "image": "jusschwa/unbound-reversecache:latest",
            "ports": [
              {
                "containerPort": 53,
                "protocol": "UDP"
              },
              {
                "containerPort": 53,
                "protocol": "TCP"
              }
            ],
            "envFrom": [
              {
                "secretRef": {
                  "name": "redis-pass"
                }
              }
            ],
            "volumeMounts": [
              {
                "mountPath": "/opt/guardian",
                "name": "guardian-conf-volume"
              }
            ]
          }
        ],
        "volumes": [
          {
            "name": "guardian-conf-volume",
            "configMap": {
              "name": "guardian-conf"
            }
          }
        ]
      }
    }
  }
}