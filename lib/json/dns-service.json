{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "dns",
    "labels": {
      "app": "dns"
    }
  },
  "spec": {
    "type": "ClusterIP",
    "selector": {
      "app": "dns"
    },
    "ports": [
      {
        "name": "dns-tcp",
        "protocol": "TCP",
        "port": 53,
        "targetPort": 9053
      },
      {
        "name": "dns-udp",
        "protocol": "UDP",
        "port": 53,
        "targetPort": 9053
      }
    ]
  }
}